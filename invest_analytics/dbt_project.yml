name: 'invest_analytics'
version: '1.0.0'
config-version: 2

profile: 'invest_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

# Configuração de Modelos e Camadas
models:
  invest_analytics:
    # Camada Silver: Dados limpos e padronizados
    staging:
      +materialized: view
    
    # Camada Gold: Indicadores de Performance e Agregados
    marts:
      +materialized: table
      +schema: gold
      +cluster_by: ["data_pregao", "ticker"] # O BigQuery organiza os dados fisicamente por esses campos
      +partition_by:
        field: data_pregao
        data_type: date
        granularity: day